package {{LowerCase .UsecaseName}}

import (
	"{{.PackagePath}}/shared/lazygo"
	"testing"
)

func TestUsecases(t *testing.T) {
	lazygo.RunTestcaseScenarios(t, func(o Outport) Inport { return NewUsecase(o) },
		scenario0(t),
	)
}
